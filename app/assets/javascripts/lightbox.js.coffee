$(document).on 'page:change', ->
  # if $('body').hasClass '_is_lightbox_mode'
  #   lightbox_width = $('#lightbox_player').outerWidth()
  #   lightbox_heght = $('#lightbox_player').height()
  #   padding = 70

    # has thumbnail
    # if $('.display_image_container').length > 0
    #   $('#lightbox_text_container').width(lightbox_width - padding - $('.display_image_container').width())